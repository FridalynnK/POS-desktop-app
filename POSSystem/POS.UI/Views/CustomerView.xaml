<UserControl x:Class="POS.UI.Views.CustomerView"
        Background="{StaticResource BgPage}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">





    <Grid Margin ="24">
    
    <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- ── LEFT: Customer List ── -->
        <Border Grid.Column="0" Background="{StaticResource BgCard}"
                CornerRadius="10" Padding="16">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="CUSTOMERS"
                           FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource TextMuted}"
                           Margin="0,0,0,12"/>
                <DataGrid ItemsSource="{Binding Customers}"
          SelectedItem="{Binding SelectedCustomer}"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          GridLinesVisibility="None"
          HeadersVisibility="Column"
          Background="Transparent"
          RowBackground="Transparent"
          AlternatingRowBackground="#1A1F2E"
          BorderThickness="0"
          CanUserResizeRows="False"
          Foreground="White">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Foreground"       Value="Black"/>
                            <Setter Property="FontWeight"       Value="SemiBold"/>
                            <Setter Property="FontSize"         Value="12"/>
                            <Setter Property="Padding"          Value="4,6"/>
                            <Setter Property="BorderThickness"  Value="0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2A2F42"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="4,2"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name"    Binding="{Binding Name}"    Width="*"/>
                        <DataGridTextColumn Header="Phone"   Binding="{Binding Phone}"   Width="120"/>
                        <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <!-- ── RIGHT: Add / Edit Form ── -->
        <Border Grid.Column="2" Background="{StaticResource BgCard}"
                CornerRadius="10" Padding="20">
            <StackPanel>
                <TextBlock 
                           FontSize="14" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimary}"
                           Margin="0,0,0,16">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="ADD CUSTOMER"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                    <Setter Property="Text" Value="EDIT CUSTOMER"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Text="Full Name *" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         Background="{StaticResource BgRow}"
                         Foreground="{StaticResource TextPrimary}"
                         BorderBrush="{StaticResource BgDivider}"
                         Padding="8,6" FontSize="13" Margin="0,0,0,12"/>

                <TextBlock Text="Phone" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                         Background="{StaticResource BgRow}"
                         Foreground="{StaticResource TextPrimary}"
                         BorderBrush="{StaticResource BgDivider}"
                         Padding="8,6" FontSize="13" Margin="0,0,0,12"/>

                <TextBlock Text="Address" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                         Background="{StaticResource BgRow}"
                         Foreground="{StaticResource TextPrimary}"
                         BorderBrush="{StaticResource BgDivider}"
                         Padding="8,6" FontSize="13" Margin="0,0,0,12"/>

                <TextBlock Text="Notes" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                         Background="{StaticResource BgRow}"
                         Foreground="{StaticResource TextPrimary}"
                         BorderBrush="{StaticResource BgDivider}"
                         Padding="8,6" FontSize="13" Margin="0,0,0,20"
                         Height="70" TextWrapping="Wrap" AcceptsReturn="True"/>

                <!-- Buttons -->
                <Button Content="💾  Save Customer"
        Command="{Binding SaveCommand}"
        FontWeight="SemiBold"
        FontSize="13"
        BorderThickness="0"
        Padding="0,10"
        Cursor="Hand"
        Margin="0,0,0,8">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1A6FBF"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#145A9E"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="🗑  Delete"
        Command="{Binding DeleteCommand}"
        FontSize="13"
        BorderThickness="0"
        Padding="0,10"
        Cursor="Hand"
        Margin="0,0,0,8">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value= "Bisque"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#C0392B"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#96281B"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#555555"/>
                                    <Setter Property="Foreground" Value="#888888"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="＋  New Customer"
        Command="{Binding NewCommand}"
        FontSize="13"
        BorderThickness="0"
        Padding="0,10"
        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource BgRow}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2A2F42"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#1A1F2E"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{StaticResource AccentGreen}"
                           FontSize="12" Margin="0,16,0,0"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
